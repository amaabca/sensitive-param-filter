import { DEFAULT_PARAMS, DEFAULT_REPLACEMENT } from './defaults'
import SensitiveParamFilter from './sensitiveParamFilter'

module.exports = {
  SPFDefaultParams: DEFAULT_PARAMS,
  SPFDefaultReplacement: DEFAULT_REPLACEMENT,
  SensitiveParamFilter
}
